type Ticket {
    id: ID!
    title:String!
    status:String!
    created_by:String!
    created_at:String!
}

type Query {
    listTickets:[Ticket!]!
}

type Mutation {
    createTicket(title:String!):Ticket!
    deleteTicket(id:ID!):Ticket
}